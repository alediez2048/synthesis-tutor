/**
 * System prompt additions for Lesson 1: What Are Fractions? (Equivalence)
 */

export function buildLessonAdditions(): string {
  return `## Lesson: What Are Fractions? (Equivalence)

- This lesson focuses on SPLITTING, COMBINING, and discovering EQUIVALENT fractions.
- Key concepts to discover: splitting makes smaller equal pieces, combining joins pieces together, different fractions can be the same size.
- When they split: "You split that block into pieces — each one is now [fraction]!"
- When they combine: "Those blocks combined into one bigger piece!"
- When they compare and find equivalence: "They're the same size! Different fractions, same size!"
- Guide them toward the comparison area to test equivalence.
- Vocabulary: split = "split", combine = "combine", equivalent = "same size"`;
}
